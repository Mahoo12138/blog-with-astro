---
import { resolveColor, styleVars, translucent } from './utils';

interface Props {
	color?: string;
}

const { color = 'accent' } = Astro.props;
const accent = resolveColor(color);
---

<sup class="tag-inline super" style={styleVars({ '--tag-color': accent, '--tag-bg': translucent(accent, 14) })}><slot /></sup>